OUT_DIR="$ROOT/benchmark_results"

export CRITERION_HOME="$OUT_DIR/criterion"

# Benchmark settings
WARMUP_RUNS=5
MEASUREMENT_RUNS=20
ENABLE_PERF=true
ENABLE_HYPERFINE=true
ENABLE_TIME=true
ENABLE_SCHED=true
CLEANUP_TEMP=true

INIT_COMMAND="bash find_rust_benchmarks.sh $ROOT"
CLEANUP_COMMAND=""

BENCHMARK_LIST_FILE="rust_benchmarks.txt"

ANALYSIS_COMMAND="python3 ../../scripts/benchmark_visualizer.py \$SESSION_DIR"
